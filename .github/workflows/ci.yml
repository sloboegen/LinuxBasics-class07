name: LinuxCourse Workflow
on:
  - push
  - pull_request

jobs:
  task07:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO"

  build:
    name: Autograding
    runs-on: ubuntu-latest
    needs: task07
    steps:
      - uses: ./.github/actions/task04@
        with: 
          filename: task04-result
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: sudo apt-get install cmatrix
      - run: touch ghc-version.txt; ghc --version > ghc-version.txt
      - run: echo "autograding emulator"
